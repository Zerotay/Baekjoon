#include <stdio.h>

int	main()
{
	int	n;
	int	arr[51][2];
	int	max, nex, ans;

	scanf("%d", &n);
	for (int i = 0; i < n; i++)
		scanf("%d %d", &arr[i][0], &arr[i][1]);
	max = 0;
	nex = 0;
	ans = 1;
	for (int i = 0; i < n; i++)
	{

	}

	return (0);
}